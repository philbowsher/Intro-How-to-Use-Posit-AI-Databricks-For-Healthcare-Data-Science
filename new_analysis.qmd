---
title: "[ TITLE — e.g. 'RTT Performance by Specialty' ]"
subtitle: "[ SUBTITLE — e.g. 'NHS Wales · Referral-to-Treatment Analysis' ]"
author: "[ YOUR NAME / TEAM ]"
date: today
format:
  html:
    toc: true
    toc-depth: 3
    theme: cosmo
    code-fold: true
    code-summary: "Show code"
    highlight-style: github
    self-contained: true
execute:
  echo: true
  warning: false
  error: true
---

<!--
═══════════════════════════════════════════════════════════════════════════════
  AI INSTRUCTIONS FOR POSIT ASSISTANT
═══════════════════════════════════════════════════════════════════════════════

  This is a Quarto document template for NHS Wales clinical analytics using
  the shared Databricks connection helper (scripts/databricks_helpers.py),
  Polars, and Plotly.

  The Setup chunk below already loads the waiting-list dataset into `df`
  (columns: Hospital, Specialty, Days_Waiting, Priority, Pathway) via
  load_waiting_list() -- from the local cache if scripts/databricks_setup.py
  has already been run, or live from Databricks otherwise. Don't rewrite
  this connection/load logic -- build analysis and charts from `df`.

  HOW TO USE THIS TEMPLATE:
  1. Fill in the YAML title/subtitle/author above
  2. Work through each section below in order, asking Posit Assistant to
     write the code for that section, then run the chunk it inserts
  3. Sections below are headers only, on purpose -- there's no placeholder
     chunk to edit. Assistant inserts a real chunk when you prompt it.

  EXAMPLE PROMPT FOR POSIT ASSISTANT:
  "Using the df DataFrame loaded above, create a Plotly bar chart showing
   average waiting days by hospital, with a dashed red line at RTT_THRESHOLD
   (126 days, the NHS 18-week RTT target). Use NHS blue (#005EB8) for the bars."

═══════════════════════════════════════════════════════════════════════════════
-->

## Overview

<!-- Ask Posit Assistant: "Write a 2-3 sentence description of what this
     document analyses, using df's columns and RTT_THRESHOLD." -->

---

## Setup

```{python}
#| label: setup
#| output: false

import sys
from pathlib import Path

sys.path.insert(0, str(Path.cwd() / "scripts"))

from databricks_helpers import load_waiting_list, RTT_THRESHOLD

df = load_waiting_list()
```

```{python}
#| label: preview

df.head()
```

---

## 1. Summary Statistics

<!-- Ask Posit Assistant: "Summarise df -- average, min, and max Days_Waiting
     overall and by Specialty, using gt or a plain Polars group_by." -->

---

## 2. Visualisation

<!-- Ask Posit Assistant: "Create a Plotly bar chart of average waiting days
     by hospital, with a dashed red line at RTT_THRESHOLD." -->

---

## 3. Breach Analysis

<!-- Ask Posit Assistant: "Show which rows in df breach RTT_THRESHOLD, and
     what proportion of referrals that represents by specialty." -->

::: {.callout-note}
## Data Privacy
All queries in this document return only aggregated or synthetic data
unless appropriate data governance approvals are in place.
:::
